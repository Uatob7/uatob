/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    appDir: true,
  },
  // reactCompiler: true, // ❌ remove this
};

export default nextConfig;